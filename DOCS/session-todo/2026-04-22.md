# Session — 2026-04-22

## Done

- **Monorepo:** Root `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`; `.gitignore` extended (`dist/`, `*.db`, pnpm store).
- **pnpm + native:** Root `.npmrc` sets `ignore-scripts=false` so `better-sqlite3` can compile (required for local and CI; pnpm 10 can skip install scripts).
- **MagicBall** (`apps/magicball`): Fastify + `better-sqlite3`, schema per design doc, publish routine (setTimeout recursion, filters, `interval_seconds_used` = wait that *just* fired), REST API, seed/reset CLIs, `GET /health`, `GET /logs` merge.
- **UI doc:** [ui.md](application_design/ui.md) updated from Vite to **Next.js + shadcn** (aligns with workspace rule and product decision).

## Decisions and reasoning

| Decision | Why |
|----------|-----|
| Fastify | Strict typing, pino, schema-friendly routes, plugin boundaries. |
| `better-sqlite3` + WAL | Synchronous, simple; WAL avoids reader/writer lock between API and publish loop. |
| `interval_seconds_used` = preceding timer, not the *next* random draw | Matches schema comment “interval that *preceded* this publish”. |
| Seed default dataset path from `apps/magicball/scripts/` | Use `../../../Temp/...` to reach repo `Temp/dataset_magicball/`. |
| `MAGICBALL_DB_PATH` in seed if `--db` omitted | Matches ops expectation; `--db` still wins. |
| `ignore-scripts=false` in repo | Without it, pnpm 10 may skip `better-sqlite3`’s build → runtime “Could not locate bindings”. |

## Deferred (unchanged from design)

- Granular publish controls UIs; MagicBall data reset remains **CLI only** (no `DELETE` API).

## Downstream contracts

- **Data Scout:** `GET /events?since=` returns **flat** fields + `tags: string[]`; `id` = MagicBall / source dataset UUID. Enum values must stay aligned with [data-scout.md](application_design/data-scout.md) and ingest schema.

---

## Done (continued — data-scout)

- **Data Scout** (`apps/data-scout`): Fastify + `better-sqlite3` + `ioredis`, full schema per design doc (`ingested_events`, `poll_state`, `poll_log`), poll loop (setTimeout recursion, `POLL_INTERVAL_SEC`), dedup by `source_event_id`, UUID lineage assignment via `node:crypto.randomUUID()`, Redis XADD to `events:ingested`, REST API (`GET /health`, `GET /events/:event_id`, `GET /events/source/:source_event_id`, `GET /logs`), reset CLI.
- **Root `package.json`:** Added `dev:datascout` script.

## Decisions and reasoning (data-scout)

| Decision | Why |
|----------|-----|
| `ioredis` as Redis client | TypeScript-friendly; clean stream (XADD) API; `enableOfflineQueue: false` for fail-loud emits |
| Named import `{ Redis }` from `ioredis` | Default import under `moduleResolution: NodeNext` resolves incorrectly for `new Redis()` — named import matches package exports |
| `node:crypto.randomUUID()` for `event_id` | Node 22+ native — no extra dependency for UUID generation |
| `schedule(0)` on first tick | First run has no cursor; immediately fetch all available events from MagicBall |
| `enableOfflineQueue: false` on Redis | Emit errors throw immediately, caught per-event; `emitted_at` stays null rather than silently queuing against a dead connection |
| Reset script included | Dev replay requires resetting Data Scout alongside MagicBall — dedup table would block re-ingestion of same `source_event_id` otherwise |
| Tags re-stringified on insert | MagicBall API returns `tags: string[]`; SQLite has no array type — `JSON.stringify` on write, parse on API read (same pattern as MagicBall) |

## Open (post data-scout)

- `scripts/start.sh` **implemented** — see design-brief and repo root; optional `SKIP_INTELLIGENCE=1` when Mongo/Gemini unavailable
- `INTERVAL_MIN_SEC` / `INTERVAL_MAX_SEC` in `apps/magicball/.env.example` are not read by the running process (interval is DB-driven via `publish_controls`) — misleading ops doc; fix in a focused cleanup pass

## Open issues — system consequences (data-scout & pipeline)

**First order**

- Data Scout runs, polls MagicBall, dedups, assigns `event_id`, persists, emits to Redis; `dev:datascout` works given Redis + MagicBall.

**Second order**

- Intelligence Layer **is implemented** (`apps/intelligence-layer`): consumes `events:ingested` via `XREADGROUP`, uses Data Scout `GET /events/:event_id`, persists all pipeline + `ai_logs` to MongoDB, exposes `GET /pipeline*`, `GET /logs`, `GET /health`; `gemini-connector.json` runtime copy in app; orchestration is sequential TypeScript (design-equivalent to LlamaIndex workflow spec).
- Partial failure: if Redis emit fails, `emitted_at` stays `null` — downstream must **not** assume every ingested row has a corresponding stream message unless retry or reconciliation is added (deferred in design).

**Third order**

- `event_id` stability is a **system invariant** — changing generation or resetting only one side of the chain (MagicBall vs Data Scout vs future Mongo) risks orphan or duplicate pipeline data.
- Reset scripts per service are the right mental model; a **documented full reset order** (and/or a single orchestrated reset) would reduce ops mistakes — still open; `scripts/start.sh` now covers the first three services in order.

---

## Done — Intelligence Layer (`apps/intelligence-layer`)

- New Fastify service: MongoDB (`INTEL_MONGO_URI` / `INTEL_MONGO_DB`), Redis consumer group on `events:ingested`, `@google/generative-ai` with manifest from `apps/intelligence-layer/gemini-connector.json` (twin of `DOCS/.../gemini-connector.json`).
- Pipeline: synthesis → impact → validation → relevance (HTTP to Notification Gateway when present) → optional `events:notification_signal` emit; fail-fast, no manifest retries; step docs on failure.
- API: `GET /pipeline`, `GET /pipeline/:event_id`, `DELETE/POST` replay routes per [intelligence-layer.md](application_design/intelligence-layer.md); `GET /logs` (ai_logs; `level=error` maps to `status=failed`); `GET /health` (mongo ping + `last_pipeline_at` + last stream outcome).
- Tests: `vitest` (manifest load, coerce validation, step name list); `pnpm --filter intelligence-layer test`.
- Root script: `dev:intelligence`; orchestration: [scripts/start.sh](../../scripts/start.sh) with `SKIP_INTELLIGENCE` optional.
- **Docs updated:** `intelligence-layer.md`, `gemini-connector.md`, `design-brief.md` (start script + prereqs), `notification-gateway.md` (IL `GET /pipeline/:event_id` aggregate; deferred `pipeline_notification_signals` clarified).

### Reasoning (why this direction)

| Direction | Why |
|----------|-----|
| Sequential `runIntelligencePipeline` instead of adding `@llamaindex/workflow` immediately | Same persistence and fail-fast invariants; fewer moving deps; doc updated to state equivalence; can swap runner later. |
| Runtime `gemini-connector.json` under `apps/intelligence-layer/` | Avoid `cwd`/`GEMINI_MANIFEST_PATH` confusion vs docs-only file; cloud/local differ only by env. |
| `XREADGROUP` + terminal-state skip | Idempotent skip when a run already finished (validation skip, relevance done, or failed doc) to tolerate redelivery. |
| Stricter `exactOptionalPropertyTypes` / plugin opts | Aligns with `tsconfig.base.json`; avoids silent `undefined` in Fastify register options. |

### Rejected / deferred

- **Notification Gateway** — **implemented** in `apps/notification-gateway` (later pass); seed users before E2E; relevance still fails fast if gateway down when IL runs alone.
- **Full log merge** of non-AI step failures into IL `GET /logs` — still deferred; use `GET /pipeline/:event_id` for step failures.

---

## Open — reconciliation checklist (docs vs code)

Use this when planning follow-up PRs; **update the table** when code or docs change.

| Doc / contract | Location (design) | Code today | Open action |
|----------------|-------------------|------------|-------------|
| Data Scout `GET /logs?level=&event_id=&page=&limit=` | [data-scout.md](application_design/data-scout.md) | [apps/data-scout/src/routes/logs.ts](../../apps/data-scout/src/routes/logs.ts) — merge `poll_log` + `event_ops_log`; filters implemented | **Done** — trace-by-event via `event_ops_log` after emit. |
| MagicBall `GET /logs` query params | [magicball.md](application_design/magicball.md) | [apps/magicball/src/routes/logs.ts](../../apps/magicball/src/routes/logs.ts) — `level`, `event_id`, `page`, `limit` | **Done** |
| IL `GET /logs` normalized shape | [intelligence-layer.md](application_design/intelligence-layer.md) | [apps/intelligence-layer/src/routes/logs.ts](../../apps/intelligence-layer/src/routes/logs.ts) — `IntelLogEntry[]`; `level=info` → `status=success` in Mongo | **Done** for aggregator; step failures still primarily via `GET /pipeline/:event_id`. |
| Admin log merge (gateway → each service) | [notification-gateway.md](application_design/notification-gateway.md) | [apps/notification-gateway/src/routes/admin.ts](../../apps/notification-gateway/src/routes/admin.ts) — `GET /admin/logs` merges upstream `/logs` | **Done** (dev-scale: fetches up to 500 rows per upstream); production should use cursors or smaller windows later. |
| Session-todo path for agents | [.cursor/rules/strict-nextjs-engineering.mdc](../../.cursor/rules/strict-nextjs-engineering.mdc) | Rule updated to `DOCS/session-todo/` | **Done** |
| `scripts/start.sh` health URLs | [design-brief.md](application_design/design-brief.md) | [scripts/start.sh](../../scripts/start.sh) — `127.0.0.1:4100–4103` when full stack | Still **local dev only**; cloud uses env-injected URLs per service. |
| `SKIP_INTELLIGENCE` / `SKIP_NOTIFICATION_GATEWAY` | design-brief + start.sh | Skip IL after Data Scout; skip GW after IL | Documented; no mock gateway inside IL. |

---

## Open issues — first / second / third order (concise)

**First order (single run / direct effects)**

- End-to-end for one `event_id`: ingest → IL pipeline (Gemini steps) → optional `events:notification_signal` **requires** MongoDB, Redis, valid `GEMINI_API_KEY`, and for a **full** path an available **Notification Gateway** at `NOTIFICATION_GW_BASE_URL` (relevance HTTP).
- `SKIP_INTELLIGENCE=1` in [scripts/start.sh](../../scripts/start.sh) prevents starting IL; it does **not** supply a mock gateway for runs you start with `pnpm dev:intelligence` alone.

**Second order (cross-service / ops)**

- **Notification Gateway** exists in `apps/notification-gateway`; full happy path requires seeded users + Redis + IL + Mongo + `GEMINI_API_KEY`.
- **Aggregated admin logs** work across services with normalized `GET /logs` shapes; IL still does not embed non-`ai_logs` pipeline failures in `/logs`.
- **IL `GET /logs`** remains AI-call-centric; use **`GET /pipeline/:event_id`** for step-level failure inspection.

**Third order (long-term / discipline / product rules)**

- **Lineage + replay + future A/B** depend on consistent `event_id`, `pipeline_version`, and coordinated resets across MagicBall SQLite, Data Scout SQLite, and IL Mongo — operator discipline and clear runbooks.
- **Do not** add silent fallbacks or masked retries in the pipeline without an **explicit** product decision and doc update — that would conflict with stated fail-fast rules.
- **Config discipline:** local vs cloud should differ by **env only** (URIs, secrets, `GEMINI_MANIFEST_PATH` as absolute in prod); [apps/intelligence-layer/.env.example](../../apps/intelligence-layer/.env.example) is the template; `start.sh` is not a production entrypoint.

---

## Done — Intelligence Layer coherence check (2026-04-22)

Cross-system coherence review of [intelligence-layer.md](application_design/intelligence-layer.md) against code, upstream (Data Scout), downstream (Notification Gateway), UI, and design-brief.

**Fixes applied to intelligence-layer.md:**

| Issue | Fix |
|-------|-----|
| `ai_logs` schema missing `max_output_tokens`, `status`, `error` fields (present in code and gemini-connector.md) | Added all three fields to schema |
| `GET /pipeline/:event_id` can return `outcome: "incomplete"` — not documented; UI only shows 3 values | Added note with definition and mapping behaviour |
| `POST /pipeline/.../replay/:step` version constraint not documented | Added note: `pipeline_version` must match running `PIPELINE_VERSION` |
| `NOTIFICATION_GW_BASE_URL` had no note that it is required for full pipeline | Added inline warning to env var table |

**Verified clean / no action needed:**

- Upstream: Data Scout XADD emits both `event_id` + `source_event_id` — IL consumes both correctly
- Category and severity enums consistent MagicBall → IL synthesis → Gateway
- `GET /pipeline/:event_id` response shape (`steps.*` envelope) matches Notification Gateway's fetch contract
- All service URLs are env-driven with localhost defaults — local/cloud parity is config-only (no code changes needed)
- Temperatures in runtime `gemini-connector.json` match docs (0.3 / 0.2 / 0.1)

**Still open (pre-existing):**

- `GET /logs` surfaces only `ai_logs` — pipeline step failures visible only via `GET /pipeline/:event_id`
- UI (Next.js) not in repo yet — gateway auth/APIs unblocked for it

---

## Done — Notification Gateway + log contract alignment (implementation pass)

**Why this direction**

| Decision | Why |
|----------|-----|
| New app `apps/notification-gateway` mirroring MagicBall/Data Scout | Single Fastify + SQLite + Redis pattern; avoids a fifth architectural style. |
| `processed_signals` keyed by Redis stream id | Idempotent delivery per message; replay / redelivery does not duplicate `notification_log` for the same stream entry. |
| Async `planDeliveryLogRows` + sync `db.transaction` persist | `better-sqlite3` transactions are synchronous; fetch IL pipeline outside, then commit all inserts atomically. |
| `GET /users` dual mode (internal query params vs admin JWT list) | Preserves existing IL `fetchUsersForRelevance` without auth; admin list stays protected. |
| Data Scout `event_ops_log` | Enables `event_id` tracing in `GET /logs` without overloading `poll_log` summary rows. |
| IL `/logs` returns `IntelLogEntry` | Same merge shape as other services for `GET /admin/logs`. |
| `NOTIF_GW_PUBLIC_URL` | Admin self-calls use env-only base URL (local `127.0.0.1` vs cloud public URL) with no code branch. |

**Artifacts**

- [apps/notification-gateway](../../apps/notification-gateway) — HTTP API, Redis consumer `notification-gateway` on `events:notification_signal`, seed/reset, vitest.
- [scripts/start.sh](../../scripts/start.sh) — fourth service + `SKIP_NOTIFICATION_GATEWAY`.
- Root [package.json](../../package.json) — `dev:notification-gateway`.
- Upstream: [apps/magicball/src/routes/logs.ts](../../apps/magicball/src/routes/logs.ts), [apps/data-scout](../../apps/data-scout) (`event_ops_log`, logs route), [apps/intelligence-layer/src/routes/logs.ts](../../apps/intelligence-layer/src/routes/logs.ts), [aiLogsRepo](../../apps/intelligence-layer/src/repositories/aiLogsRepo.ts).

**Verification**

- `pnpm --filter notification-gateway test`
- `pnpm --filter intelligence-layer test`
- Manual: seed gateway → `pnpm dev:stack` → one event through pipeline → `notification_log` rows + `processed_signals`

---

## Done — UI (`apps/ui`) — Next.js 15 + shadcn

**Artifacts**

- [apps/ui](../../apps/ui) — App Router, JWT HTTP-only cookie (`sonrise_jwt`), Route Handlers proxy to `NOTIF_GW_URL` / `INTEL_URL`, Edge `middleware.ts` for auth + admin/pipeline API gates, pages: `/login`, `/alerts`, `/admin/pipeline`, `/admin/pipeline/[event_id]`, `/admin/logs`, `/admin/users`, `/admin/system`.
- [scripts/start.sh](../../scripts/start.sh) — fifth step: UI on `4104`, health `GET /api/health`; `SKIP_UI=1` to run backend only.
- Root [package.json](../../package.json) — `dev:ui`.
- Docs: [ui.md](../application_design/ui.md) (env vars `NOTIF_GW_URL` / `INTEL_URL`, architecture notes), [design-brief.md](../application_design/design-brief.md) (full stack + `SKIP_UI`).

**Decisions and why**

| Decision | Why |
|----------|-----|
| Proxy all service calls via `/api/*` | Service URLs stay server-only; no `NEXT_PUBLIC_*` for backends; aligns with “no secrets in browser” and future CORS tightening. |
| HTTP-only cookie for JWT | SSR + `cookies()` in Route Handlers; not exposed to XSS via `localStorage`. |
| Middleware decodes JWT payload only (no verify) | UI must not embed `JWT_SECRET`; services enforce Bearer auth. |
| `GET /api/health` for `wait_health` | Next has no root `/health`; curl target is explicit and stable. |
| Middleware blocks non-admin on `/api/pipeline*`, `/api/admin/*`, `GET /api/users` | Defense in depth with page-level `/admin` layout; avoids relying on IL’s open pipeline routes for unauthenticated browsers. |
| Delete step UI for `completed` **or** `emitted` | Notification signal documents use `emitted`, not `completed`. |
| Logs filter form uses native `<select>` | Radix `Select` does not participate in GET form submit; native controls preserve “no client-side filter merge” contract. |

**Open (follow-up)**

- Optional: shared `/docs/` mirror of `DOCS/` per strict rule — not done in this pass.
- Reconciliation table row: add `scripts/start.sh` now includes UI and `SKIP_UI` (update “Open — reconciliation checklist” when next editing that table).

---

## Verified — Coherence plan execution (2026-04-22)

Coherence plan at `/Users/dstfd/.claude/plans/i-want-you-to-tingly-parnas.md` was confirmed executed in a prior session. This pass verified all artifacts build and test cleanly.

**IL `findAiLogs` pagination fix — confirmed:**
- `apps/intelligence-layer/src/repositories/aiLogsRepo.ts`: `level=info` maps to `filter[“status”] = “success”` in MongoDB query; `warn`/`debug` returns `{ _id: { $exists: false } }` (empty result, correct total). Fix is at the DB layer — no post-fetch JS filter needed.
- `apps/intelligence-layer/src/routes/logs.ts`: no compensating post-fetch filter; returns `{ data, page, limit, total }` directly.
- **Why:** `total` was returning the page count (not the DB total) for `level=info` queries — broken `GET /admin/logs` pagination aggregation from the gateway.

**Notification Gateway — confirmed:**
- All 31 files present under `apps/notification-gateway/`.
- `pnpm --filter notification-gateway build` — exits 0, zero TypeScript errors.
- `pnpm --filter notification-gateway test` — 3 test files, 4 assertions, all pass.
- `pnpm --filter notification-gateway seed` — 5 users written to `notifgw.db`.
- `pnpm --filter intelligence-layer build` and `pnpm --filter intelligence-layer test` — unaffected, still clean.

**Verification invariants confirmed:**
- `bcryptjs` (pure JS) — no native build step; `pnpm install` resolves without errors.

---

## Done — Pipeline admin: source event + list dedupe + synthesis JSON diagnostics (2026-04-22)

**Problem**

- Admin pipeline list used React `key={event_id}` while `GET /pipeline` returned **multiple `pipeline_synthesis` documents per `event_id`** (failed retries / replays) → duplicate key console errors.
- Operators could not see **raw Data Scout event JSON** next to synthesis output on the detail page.
- `ai_logs` showed `Unparseable JSON: Unexpected end of JSON input` with **empty `response`** — root cause is `JSON.parse("")` after an **empty model text** response, not schema coercion in `coerceSynthesis`.

**Root cause (evidence from MongoDB `intelligence` DB, local)**

- `pipeline_synthesis` for `event_id` `4efef09b-4a67-4002-a5fa-04a0509ac206`, `pipeline_version` `1.0`: **2 documents** — `processed_at` `2026-04-22T15:46:15.997Z` and `2026-04-22T15:50:20.493Z`, both `status: failed`, errors **429 Too Many Requests** from `https://generativelanguage.googleapis.com/...` (Gemini Developer API quota), not Vertex.
- `ai_logs` for synthesis JSON failures (e.g. `a977d117-b542-4673-91bc-94eeadeeaa78`): `response` length **0**, `tokens` **0**, `duration_ms` **21521**, error `Unparseable JSON: Unexpected end of JSON input` — consistent with empty candidate text.

**Solution**

- `listSynthesisForRuns` / `countSynthesis`: Mongo aggregation — **latest document per `event_id`**, then paginate; `total` = distinct `event_id` count.
- `GET /pipeline/:event_id`: fetch Data Scout event, return `source_event` + `source_event_fetch_error`.
- UI: “Source event (Data Scout)” card on pipeline detail; table row keys `event_id:processed_at` as a safety net.
- `geminiConnector`: reject **empty** model text with `finish_reason` in the error; unwrap stringified `"output"` when the model returns JSON as a string; guard `JSON.parse` on empty trimmed string.
- Docs: [intelligence-layer.md](application_design/intelligence-layer.md) list/detail contract updated.
- `GET /users/me` registered before `GET /users/:user_id` in `routes/users.ts` — parametric capture of “me” prevented.
- `notification_log.status` uses `”mocked”` (never `”success”`) — design invariant enforced in `deliveryOrchestrator.ts`.
- Failed deliveries NOT XACKed — message stays in Redis PEL for operator retry.
- `processed_signals` table provides idempotency on redelivery.

---

## Pipeline run detail: AI call sent / received (admin UI + IL)

**Problem**

- Admins had full step JSON in `StepCard` and Mongo `ai_logs`, but no dedicated **sent (prompt) / received (response)** view tied to a run.

**Root cause**

- `GET /pipeline/:event_id` does not join `ai_logs` (keeps the aggregate small for **Notification Gateway** and others).

**Solution**

- **Intelligence Layer:** `GET /pipeline/:event_id/ai-log/:ai_log_id` — `findAiLogById` + return `404` if doc missing or `event_id` mismatch (IDOR).
- **Next.js:** `GET /api/pipeline/.../ai-log/...` → `proxyIntel`.
- **UI:** `StepCard` — collapsible **“AI call: sent & received”** for `synthesis` | `impact_evaluation` | `validation` when `ai_log_id` is set; lazy fetch on first open.
- **Docs:** [ui.md](application_design/ui.md), [intelligence-layer.md](application_design/intelligence-layer.md).

**Reasoning (directions)**

| Direction | Why |
|-----------|-----|
| Separate `ai-log` route vs embedding in `GET /pipeline` | Downstream `fetchPipelineAggregate` stays unchanged; no large prompt/response on every delivery path. |
| `event_id` in path with mismatch → 404 | Prevents IDOR on guessed ObjectIds. |
| Lazy load in the client | First paint of run detail stays fast. |

**Verification**

- Typecheck / lint on touched `apps/ui` and `apps/intelligence-layer` packages.
- Admin: open a run with a completed AI step, expand “AI call”, confirm prompt/response match Mongo `ai_logs` for that `ai_log_id`.
