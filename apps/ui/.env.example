# Service base URLs — server-side only (Next.js Route Handlers proxy); never exposed to the browser
NOTIF_GW_URL=http://localhost:4103
INTEL_URL=http://localhost:4102

# Port for the Next.js dev / start server (see package.json scripts)
UI_PORT=4104

# Cookie `Secure` flag is set when NODE_ENV=production
