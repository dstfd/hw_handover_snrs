# Local AI/editor settings (absolute paths, machine-specific)
.claude/settings.local.json

# OS
.DS_Store

# Common local secrets and deps (if added later)
.env
.env.*
!.env.example
node_modules/

# Build artifacts
dist/
.next/
*.tsbuildinfo

# SQLite databases (runtime state)
*.db
*.db-shm
*.db-wal

# pnpm
.pnpm-store/

# Root-level log dumps only (do not use `logs/` — it would ignore Next.js `*/logs/` route segments)
/logs/
Temp/